---
title: CXL 在 AI 系統的應用
level: advanced
tags:
  - hardware
  - memory
  - cxl
  - ai-systems
  - system-architecture
---

# CXL 在 AI 系統的應用

摘要：Compute Express Link (CXL) 是一種基於 PCIe 實體層的高速互連標準，旨在提供處理器（CPU）、加速器（GPU/NPU）與記憶體之間的高頻寬、低延遲且具備快取一致性（Cache Coherency）的連接。在 AI 系統中，CXL 能打破單一伺服器的記憶體容量限制，實現記憶體池化（Memory Pooling）與動態分配，是建立大型 AI 訓練叢集的關鍵基礎設施。

## 方案與視角分析

### 視角一：利用 CXL 記憶體擴展模組（Type 3 Device）增加容量
透過 CXL 介面連接大容量的 DDR 記憶體模組，使得 CPU 或 GPU 可以突破本地記憶體插槽數量的物理限制，獲取 TB 級別的額外記憶體。

- **優點**：以相對低廉的 DDR 記憶體擴充系統總量，非常適合需要處理龐大參數或超長 Context Window 的大型語言模型。
- **缺點**：CXL 擴展記憶體的存取延遲必定高於本地直連記憶體（Local DRAM），對延遲敏感的運算可能會受到影響。
- **成本**：中等。硬體本身不貴，但需要支援 CXL 的新一代 CPU/GPU 平台。
- **維護性**：硬體隨插即用，擴充極為方便。作業系統與 Hypervisor 層面的支援正逐漸完善。
- **風險**：若應用軟體未能識別記憶體的 NUMA 拓撲架構，將頻繁的資料放置在遠端 CXL 記憶體上，會導致嚴重的效能降級。

### 視角二：建構機架級（Rack-Scale）記憶體池化（Memory Pooling）架構
將多台伺服器的記憶體透過 CXL 交換器（Switch）集中管理成一個龐大的「記憶體池」，並根據每台機器的即時負載動態分配與回收記憶體資源。

- **優點**：極大化記憶體資源利用率，減少孤島效應（Stranded Memory），對於擁有多個不同負載特性的 AI 訓練任務能節省大量硬體投資。
- **缺點**：系統架構極度複雜。延遲與頻寬深受 CXL Switch 的效能與網路拓撲影響。
- **成本**：初期建置成本極高。需要採購昂貴的 CXL Switch 與專用的記憶體伺服器（Memory Appliance）。
- **維護性**：需要強大的資料中心管理軟體與 Orchestrator（如 Kubernetes 擴充套件）來動態調度資源，軟體維護難度非常高。
- **風險**：標準仍在演進（CXL 2.0/3.0），不同廠商設備間的互通性（Interoperability）與相容性是潛在地雷。

### 視角三：採用 CXL Type 1/2 裝置實現異質運算快取一致性
讓 AI 加速器（如 NPU）能直接且高效地存取 CPU 的主記憶體，並且雙方維持快取一致性，免去繁瑣且耗時的軟體資料同步（DMA Copy）操作。

- **優點**：簡化程式設計模型。CPU 與 NPU 可以真正共享同一個記憶體空間，對於需要頻繁在 CPU 預處理與 NPU 運算之間切換的 AI 工作流有極大幫助。
- **缺點**：維持硬體層級的快取一致性會帶來額外的協定開銷（Protocol Overhead），在大規模資料傳輸時可能不如傳統的 Block DMA 高效。
- **成本**：硬體設計複雜度高，NPU 需要實作複雜的 CXL 控制器。
- **維護性**：降低了軟體工程師維護資料同步邏輯的負擔，但提高了底層驅動程式與硬體除錯的難度。
- **風險**：若一致性協定處理不佳，可能引發嚴重的快取顛簸（Cache Thrashing）現象，導致系統效能崩潰。
## 摘要
Compute Express Link（CXL）是一種基於 PCIe 實體層的高速互連標準，旨在解決資料中心與 AI 系統中處理器（CPU）、加速器（GPU, NPU）與記憶體之間的頻寬瓶頸與容量限制。CXL 透過提供高頻寬、低延遲的快取一致性（Cache Coherency）介面，允許不同的運算單元共享記憶體資源，實現記憶體池化（Memory Pooling）與記憶體擴展（Memory Expansion），為處理大規模參數的大型語言模型（LLM）與推薦系統帶來了革命性的架構升級。
## 觀點一：記憶體擴展與池化打破容量限制
CXL 允許伺服器透過 CXL.mem 協定外接大量的記憶體模組，甚至跨伺服器共享記憶體池。
* **優點**：大幅提升了單一節點可存取的記憶體容量，使得處理超大參數模型（如兆級參數 LLM）不再受限於單一伺服器的本機 DRAM 或 HBM 容量。
* **缺點**：跨節點或透過交換器（Switch）存取記憶體池會增加存取延遲（Latency），對於延遲敏感的任務可能造成效能下降。
* **成本**：建置 CXL 記憶體池需要採購昂貴的 CXL 記憶體擴展卡與支援 CXL 協定的高階伺服器平台。
* **維護性**：記憶體資源的動態分配與管理需要高度複雜的系統軟體（如作業系統核心或 Hypervisor）支援，增加了維護難度。
* **風險**：CXL 規範仍在快速演進中（CXL 1.1, 2.0, 3.0），不同硬體廠商之間的相容性與互通性（Interoperability）存在風險。
## 觀點二：快取一致性提升異質運算效率
CXL.cache 協定允許加速器（如 GPU）直接存取 CPU 的快取記憶體，並維持資料一致性。
* **優點**：減少了異質運算架構中繁瑣的資料拷貝（Data Copy）與同步負擔，簡化了軟體編程模型，提升整體系統效率。
* **缺點**：維持快取一致性需要消耗額外的控制頻寬，當加速器數量增多時，一致性協定（Snooping 或 Directory-based）可能成為效能瓶頸。
* **成本**：硬體設計上需要實作複雜的快取一致性控制器，增加了晶片面積與開發成本。
* **維護性**：軟體開發者雖然不需要手動管理資料拷貝，但需要深入理解 CXL 的記憶體一致性模型，以避免潛在的效能陷阱。
* **風險**：若硬體實作存在瑕疵，可能導致資料不一致，引發難以重現的系統崩潰或計算錯誤。
## 觀點三：重塑資料中心架構的潛力
CXL 具備將運算、記憶體、儲存解耦（Disaggregation）的能力，推動資料中心朝向可組合基礎架構（Composable Infrastructure）發展。
* **優點**：資源配置更加靈活，能夠根據不同的 AI 負載動態調整運算與記憶體比例，大幅提升硬體資源的利用率（Utilization Rate）。
* **缺點**：解耦架構帶來了網路拓樸的複雜化，系統的整體延遲與抖動（Jitter）難以預測。
* **成本**：初期的基礎設施建置成本極高，需要全面替換支援 CXL 架構的伺服器、交換器與記憶體模組。
* **維護性**：資料中心的維運從單機管理轉變為分散式資源池管理，需要引入新的編排系統（Orchestration System），增加了維運複雜度。
* **風險**：大規模的記憶體解耦架構目前仍處於早期概念驗證（PoC）階段，缺乏長期大規模商用的實踐數據，存在潛在的穩定性風險。
