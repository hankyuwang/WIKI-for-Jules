---
title: NVM 在邊緣 AI 的發展
level: intermediate
tags:
  - hardware
  - memory
  - nvm
  - edge-ai
---

# NVM 在邊緣 AI 的發展

摘要：非揮發性記憶體（Non-Volatile Memory, NVM），如 RRAM、MRAM、PCM 等，具備斷電不遺失資料的特性，且儲存密度通常高於 SRAM。在邊緣 AI（Edge AI）領域，由於設備常面臨嚴格的功耗與體積限制，NVM 被視為取代傳統 eFlash，甚至是部分 SRAM，以實現「常時開啟 (Always-on)」、「即開即用 (Normally-off computing)」與超低功耗 AI 推論的關鍵技術。

## 方案與視角分析

### 視角一：作為 eFlash 的替代品儲存 AI 模型權重
隨著邊緣設備採用的製程微縮（如 28nm 以下），傳統的嵌入式快閃記憶體（eFlash）微縮遭遇瓶頸，MRAM 或 RRAM 成為儲存 AI 模型與韌體的理想替代品。

- **優點**：NVM 在先進製程下的微縮能力遠勝 eFlash，且寫入速度更快、功耗更低。支援 Byte-addressable（可位元組尋址），無需像 Flash 那樣進行區塊抹除。
- **缺點**：與邏輯製程的整合難度較高，需要額外的光罩（Mask）步驟。
- **成本**：相比於標準邏輯製程會增加一定的製造成本（需要 3~4 層額外的 Mask），但在先進製程中總體成本仍優於 eFlash。
- **維護性**：軟體層面幾乎可以透明替換 Flash，對現有韌體開發流程影響小，易於維護。
- **風險**：不同代工廠的 NVM 製程參數差異大，導致 IP 移植困難，存在供應商鎖定的風險。

### 視角二：利用 NVM 實現 Normally-Off Computing (常閉運算)
邊緣 AI 設備（如 IoT 感測器）經常處於休眠狀態。利用 NVM 的非揮發特性，可以將系統狀態與暫存資料直接儲存在 NVM 中，讓系統在休眠時完全切斷電源（Zero Standby Power），並在喚醒時瞬間恢復。

- **優點**：極大化延長電池壽命，對於靠能量採集（Energy Harvesting）供電的超微型 AI 設備至關重要。
- **缺點**：頻繁的喚醒與休眠需要極快的讀寫速度，目前某些 NVM 的寫入延遲與功耗仍是一大挑戰。
- **成本**：系統級電源管理設計非常複雜。
- **維護性**：需要全新的作業系統排程器與硬體中斷機制的配合，系統軟韌體的開發與維護難度急遽升高。
- **風險**：NVM 的寫入次數限制（Endurance）。若頻繁切換狀態，可能導致記憶體單元過早損壞。

### 視角三：進階至基於 NVM 的類比 CIM (記憶體內運算)
不僅用 NVM 來「儲存」資料，更利用其物理特性（如電阻矩陣）直接進行 AI 的乘加運算（MAC），也就是前沿的類比 CIM 架構。

- **優點**：理論上能達到極致的能源效率（數百 TOPS/W），徹底消除權重搬移的功耗，是邊緣 AI 的聖杯。
- **缺點**：技術極不成熟。類比計算極易受到溫度、雜訊與元件退化（Device Degradation）的影響，導致推理準確度飄移。
- **成本**：研發與流片成本極高，目前多處於學術界與新創公司的 PoC 階段。
- **維護性**：硬體難以除錯，且需要極度客製化的編譯器與訓練演算法（如 Hardware-Aware Training）來補償硬體誤差，軟硬體維護成本高昂。
- **風險**：商業化落地的風險極高。若無法解決精度下降與良率問題，將難以與成熟的數位 NPU 競爭。
## 摘要
隨著物聯網（IoT）與穿戴式裝置的普及，邊緣人工智慧（Edge AI）需要在極度受限的功耗與面積預算下執行神經網路推理。傳統基於 SRAM 與 Flash 的架構往往無法滿足「即時喚醒、超低待機功耗」的需求。新型非揮發性記憶體（Non-Volatile Memory, NVM），如電阻式記憶體（RRAM）、磁阻式記憶體（MRAM）與相變化記憶體（PCM），因具備高密度、非揮發性以及與 CMOS 製程相容的特性，正逐漸成為邊緣 AI 晶片中儲存神經網路權重與執行記憶體內運算（CIM）的關鍵技術。
## 觀點一：常啟動（Always-On）系統的超低功耗優勢
邊緣 AI 裝置（如智慧語音助理、智慧門鈴）通常有 99% 的時間處於待機狀態，只有在特定觸發條件下才會喚醒執行推理。
* **優點**：NVM 在斷電時不會遺失資料，因此系統可以在待機時完全切斷記憶體電源，將靜態功耗降至接近零；喚醒時也不需從外部 Flash 重新載入權重，達到即時啟動（Instant-On）。
* **缺點**：寫入操作非常耗電且速度慢，因此 NVM 更適合「一次寫入、多次讀取（WORM）」的推理任務，難以支援邊緣端的線上學習（Online Learning）。
* **成本**：相較於成熟的 eFlash，新型 NVM 的製造與測試成本目前仍然較高。
* **維護性**：系統韌體需要特別設計以處理極端的電源狀態轉換（Power Gating），增加了電源管理的複雜度。
* **風險**：若應用場景需要頻繁更新 AI 模型，NVM 的寫入次數限制（Endurance）可能成為產品壽命的瓶頸。
## 觀點二：與 CMOS 製程相容的嵌入式整合
新型 NVM（尤其是 MRAM 與 RRAM）通常被設計在 CMOS 邏輯電路的後段金屬層（Back-End-Of-Line, BEOL），這為晶片設計帶來了極大的彈性。
* **優點**：可以直接嵌入邏輯晶片中，不佔用底層矽基板面積，且能以微處理器（MCU）的標準製程進行整合，無需昂貴的高壓製程模組。
* **缺點**：BEOL 整合對於熱預算（Thermal Budget）有嚴格限制，以避免破壞底層的 CMOS 電晶體，這限制了某些 NVM 材料的選擇。
* **成本**：雖然單元成本較低，但需要晶圓廠（Foundry）提供客製化的 IP 與設計套件（PDK），初期導入成本不低。
* **維護性**：硬體設計者需要熟悉新的混合訊號設計流程，且缺乏標準化的測試介面。
* **風險**：不同晶圓代工廠的 NVM 製程差異極大，導致 IP 難以跨廠牌移植（Porting），增加了供應鏈鎖定（Vendor Lock-in）的風險。
## 觀點三：邊緣安全與資料隱私的保障
在邊緣端處理敏感資料（如生物辨識、醫療數據）時，硬體安全性至關重要。
* **優點**：NVM 具有物理不可複製功能（Physically Unclonable Function, PUF）的潛力，可利用其隨機變異特性產生獨一無二的硬體金鑰；且因不需外接記憶體，避免了資料在匯流排上被竊聽的風險。
* **缺點**：某些 NVM（如 MRAM）容易受到外部強磁場的干擾而導致資料損毀或洩漏。
* **成本**：為了防止物理攻擊（如侵入式探測、側信道攻擊），需要額外設計防護電路，增加了晶片面積。
* **維護性**：安全機制的設計與驗證需要專業的資安團隊介入，延長了產品上市時間（Time-to-Market）。
* **風險**：隨著攻擊技術的進步，硬體內建的安全機制若被攻破，由於無法像軟體一樣輕易發布修補程式（Patch），可能會引發嚴重的安全危機。
